#include <stdio.h>
/**
 * main - initializes the program
 * Return:zero if no errors, other value if there are errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
