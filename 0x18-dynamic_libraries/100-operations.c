#include <stdio.h>
/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 * Return: sum of the integers
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub - subtracts two integers
 * @a: first integer
 * @b: second integer
 * Return: difference of two integers
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - divides two integers
 * @a: first integer
 * @b: second integer
 * Return: division of two integers
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - determines modulus of two integers
 * @a: first integer
 * @b: second integer
 * Return: modulus of two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
